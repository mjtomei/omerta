# omerta_mesh

**Repository:** https://github.com/mjtomei/omerta_mesh

This directory is a stub that replaces a Git submodule. Only `.commit` and
`README.md` are tracked. Run `make` from the project root to clone the full
repository into this directory, which will overwrite this file with the real README.
