# omerta_protocol

**Repository:** https://github.com/mjtomei/omerta_protocol

This directory is a stub that replaces a Git submodule. Only `.commit` and
`REPO.md` are tracked. Run `make` from the project root to clone the full
repository into this directory.
